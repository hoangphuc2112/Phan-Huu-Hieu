<!DOCTYPE html>
<html class="dark" lang="en">
<head>
    <meta charset="utf-8"/>
    <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
    <title>{{ pageTitle }}</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Noto+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet"/>
    
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#a413ec",
                        "background-light": "#f7f6f8",
                        "background-dark": "#161118", 
                        "glass-dark": "rgba(255, 255, 255, 0.03)",
                        "glass-border": "rgba(255, 255, 255, 0.08)",
                    },
                    fontFamily: {
                        "display": ["Space Grotesk", "sans-serif"],
                        "body": ["Noto Sans", "sans-serif"],
                    },
                    borderRadius: {"DEFAULT": "0.5rem", "lg": "1rem", "xl": "1.5rem", "full": "9999px"},
                    backdropBlur: {
                        xs: '2px',
                    }
                },
            },
        }
    </script>
    <style>
        /* Animation chạy vô tận */
@keyframes infinite-scroll {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
}

.animate-marquee {
    display: flex;
    width: max-content; /* Quan trọng: độ rộng dựa trên nội dung */
    animation: infinite-scroll 25s linear infinite; /* Tốc độ 25s */
}

/* Hiệu ứng dừng lại khi di chuột vào */
.group:hover .animate-marquee {
    animation-play-state: paused;
}

/* Mặt nạ làm mờ 2 bên cạnh để logo ẩn hiện mượt mà */
.mask-gradient {
    mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
    -webkit-mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
}

/* Custom scrollbar */
::-webkit-scrollbar { width: 8px; }
::-webkit-scrollbar-track { background: #161118; }
::-webkit-scrollbar-thumb { background: #332839; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #a413ec; }

/* Glassmorphism */
.glass-panel {
    background: rgba(30, 30, 35, 0.4);
    backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }
        
        .glass-card:hover {
            background: rgba(255, 255, 255, 0.06);
            border-color: rgba(164, 19, 236, 0.3);
            transform: translateY(-2px);
        }

        .text-glow { text-shadow: 0 0 20px rgba(164, 19, 236, 0.5); }
    </style>
</head>
<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-white font-body min-h-screen flex flex-col overflow-x-hidden selection:bg-primary selection:text-white">

<div class="fixed inset-0 z-0 pointer-events-none overflow-hidden">
    <div class="absolute -top-[10%] -left-[10%] w-[50%] h-[50%] bg-primary/10 rounded-full blur-[120px]"></div>
    <div class="absolute bottom-[10%] right-[5%] w-[40%] h-[40%] bg-blue-600/10 rounded-full blur-[100px]"></div>
</div>

<nav class="relative z-50 w-full px-6 py-6 flex justify-center">
    <div class="glass-panel rounded-full px-6 py-3 flex items-center justify-between w-full max-w-[1200px]">
        <div class="flex items-center gap-3">
            <div class="bg-primary/20 p-2 rounded-full">
                <span class="material-symbols-outlined text-primary text-2xl">diamond</span>
            </div>
            <span class="font-display font-bold text-xl tracking-tight text-white">CREATIVE.</span>
        </div>
<div class="hidden md:flex items-center gap-8">
    <a class="text-sm font-medium text-white/80 hover:text-white transition-colors" href="/">Giới Thiệu</a>
    
    <a class="text-sm font-medium text-white/60 hover:text-white transition-colors" href="/news">Tin Tức</a>
    
    <a class="text-sm font-medium text-white/60 hover:text-white transition-colors" href="/media">Báo Chí Phỏng Vấn</a>
    <a class="text-sm font-medium text-white/60 hover:text-white transition-colors" href="/portfolio">Portfolio</a>
</div>
        <button class="bg-white text-black hover:bg-primary hover:text-white transition-colors rounded-full px-5 py-2 text-sm font-bold font-display">
            Let's Talk
        </button>
    </div>
</nav>

    <main class="relative z-10 flex flex-col items-center w-full px-4 md:px-8 pb-20">
        
        <section class="relative pt-10 pb-6 px-4 md:px-8 max-w-[1280px] mx-auto w-full">
            <div class="flex flex-col md:flex-row gap-4 items-center mb-8">
                <h1 class="text-4xl md:text-6xl font-bold text-white tracking-tight">
                    Latest <span class="text-transparent bg-clip-text bg-gradient-to-r from-white to-white/50">Stories</span>
                </h1>
                <div class="h-px bg-white/10 flex-1 ml-4 hidden md:block"></div>
                <p class="text-white/60 text-sm md:text-base max-w-md text-right hidden md:block">
                    Exploring the intersection of technology, culture, and modern society through a critical lens.
                </p>
            </div>

            <div class="group relative w-full h-[500px] rounded-2xl overflow-hidden shadow-2xl cursor-pointer">
                <div class="absolute inset-0 bg-cover bg-center transition-transform duration-700 group-hover:scale-105" 
                     style="background-image: url('{{featuredArticle.image}}');">
                </div>
                <div class="absolute inset-0 bg-gradient-to-t from-background-dark via-background-dark/60 to-transparent"></div>
                <div class="absolute bottom-0 left-0 p-8 md:p-12 w-full max-w-4xl">
                    <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-primary/20 backdrop-blur-md border border-primary/30 text-primary text-xs font-bold uppercase tracking-wider mb-4">
                        <span class="w-1.5 h-1.5 rounded-full bg-primary animate-pulse"></span>
                        Editor's Pick
                    </span>
                    <h2 class="text-3xl md:text-5xl font-bold text-white mb-4 leading-tight group-hover:text-primary transition-colors font-display">
                        {{featuredArticle.title}}
                    </h2>
                    <p class="text-white/80 text-lg mb-6 line-clamp-2 max-w-2xl">
                        {{featuredArticle.summary}}
                    </p>
                    <div class="flex items-center gap-4 text-sm text-white/60">
                        <div class="flex items-center gap-2">
                            <div class="w-8 h-8 rounded-full bg-white/10 overflow-hidden border border-white/20">
                                <img alt="Author" class="w-full h-full object-cover" src="{{featuredArticle.authorAvatar}}"/>
                            </div>
                            <span class="font-medium text-white">{{featuredArticle.author}}</span>
                        </div>
                        <span>•</span>
                        <span>{{featuredArticle.date}}</span>
                        <span>•</span>
                        <span class="text-accent-green">{{featuredArticle.readTime}}</span>
                    </div>
                </div>
            </div>
        </section>

        <div class="sticky top-0 z-40 w-full py-4 backdrop-blur-md bg-background-dark/50 border-b border-white/5 mb-8">
            <div class="max-w-[1280px] mx-auto px-4 overflow-x-auto hide-scrollbar">
                <div class="flex gap-3 min-w-max">
                    {{#each categories}}
                    <button class="flex items-center gap-2 px-4 py-2 rounded-xl {{#if this.active}}bg-white text-background-dark font-bold{{else}}glass-panel text-white font-medium hover:bg-white/10 border-white/10{{/if}} text-sm transition-colors">
                        <span class="material-symbols-outlined text-[18px] {{this.color}}">{{this.icon}}</span>
                        {{this.name}}
                    </button>
                    {{/each}}
                </div>
            </div>
        </div>

        <div class="max-w-[1280px] mx-auto px-4 md:px-8 w-full">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 auto-rows-fr">
                
                {{#each articles}}
                {{#if this.isQuote}}
                <a href="/news/{{this.slug}}" class="block group {{#if this.isLarge}}md:col-span-2{{/if}}">
            
            <article class="glass-panel glass-panel-hover rounded-2xl overflow-hidden flex flex-col transition-all duration-300 h-full">
                <div class="flex ...">
                    </div>
            </article>

        </a>
                <article class="glass-panel glass-panel-hover rounded-2xl overflow-hidden flex flex-col justify-center p-8 bg-gradient-to-br from-primary/20 to-purple-900/20 border-primary/20 md:col-span-1 group relative cursor-pointer">
                    <div class="absolute top-4 right-4 opacity-20">
                        <span class="material-symbols-outlined text-[64px]">format_quote</span>
                    </div>
                    <div class="relative z-10">
                        <span class="text-xs font-bold tracking-widest uppercase text-white/50 mb-4 block">Quote of the Day</span>
                        <h3 class="text-2xl font-bold text-white italic mb-6 leading-relaxed">
                            "{{this.content}}"
                        </h3>
                        <p class="text-primary font-bold">— {{this.author}}</p>
                    </div>
                </article>
                {{else}}
                <article class="glass-panel glass-panel-hover rounded-2xl overflow-hidden flex flex-col transition-all duration-300 group {{#if this.isLarge}}md:col-span-2{{/if}} cursor-pointer">
                    <div class="flex {{#if this.isLarge}}flex-col md:flex-row h-full{{else}}flex-col{{/if}}">
                        <div class="{{#if this.isLarge}}md:w-1/2{{else}}h-48{{/if}} relative overflow-hidden {{#if this.isLarge}}min-h-[240px]{{/if}}">
                            <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500 group-hover:scale-110" 
                                 style="background-image: url('{{this.image}}');">
                            </div>
                            <div class="absolute inset-0 bg-black/20 group-hover:bg-transparent transition-colors"></div>
                            
                            {{#unless this.isLarge}}
                            <div class="absolute top-4 right-4 bg-black/40 backdrop-blur-md px-2 py-1 rounded-md border border-white/10">
                                <span class="material-symbols-outlined {{this.categoryColor}} text-[16px]">article</span>
                            </div>
                            {{/unless}}
                        </div>

                        <div class="{{#if this.isLarge}}md:w-1/2{{/if}} p-6 md:p-8 flex flex-col justify-between flex-1">
                            <div>
                                <div class="flex justify-between items-start mb-4">
                                    <span class="text-xs font-bold tracking-widest uppercase {{#if this.categoryColor}}{{this.categoryColor}}{{else}}text-primary{{/if}}">
                                        {{this.category}}
                                    </span>
                                    <span class="text-white/40 text-xs">{{this.timeAgo}}</span>
                                </div>
                                <h3 class="{{#if this.isLarge}}text-2xl{{else}}text-xl{{/if}} font-bold text-white mb-3 leading-snug group-hover:text-primary transition-colors font-display">
                                    {{this.title}}
                                </h3>
                                <p class="text-white/60 text-sm leading-relaxed mb-6 line-clamp-3">
                                    {{this.summary}}
                                </p>
                            </div>
                            
                            <div class="flex items-center justify-between mt-auto pt-4 border-t border-white/5">
                                {{#if this.author}}
                                <span class="text-xs font-medium text-white/80">By {{this.author}}</span>
                                {{else}}
                                <div class="flex items-center gap-2 text-xs text-white/40">
                                    <span class="material-symbols-outlined text-[14px]">schedule</span>
                                    <span>Read more</span>
                                </div>
                                {{/if}}
                                
                                {{#if this.isLarge}}
                                <button class="w-8 h-8 rounded-full bg-white/10 flex items-center justify-center text-white group-hover:bg-primary transition-colors">
                                    <span class="material-symbols-outlined text-[16px]">arrow_forward</span>
                                </button>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                </article>
                {{/if}}
                {{/each}}

            </div>

            <div class="mt-16 flex justify-center">
                <button class="group flex items-center gap-3 px-8 py-3 rounded-full bg-white text-background-dark font-bold hover:bg-primary hover:text-white transition-all duration-300 transform hover:scale-105 shadow-lg shadow-white/5 hover:shadow-primary/50">
                    <span class="material-symbols-outlined group-hover:animate-spin">refresh</span>
                    Load More Articles
                </button>
            </div>
        </div>

    </main>

    <section class="relative py-20 px-4">
        <div class="absolute inset-0 overflow-hidden">
            <div class="absolute -bottom-[20%] left-[20%] w-[60%] h-[40%] bg-primary/20 rounded-full blur-[100px]"></div>
        </div>
        <div class="relative z-10 max-w-3xl mx-auto text-center">
            <div class="inline-block p-3 rounded-full bg-white/5 backdrop-blur-xl mb-6 border border-white/10">
                <span class="material-symbols-outlined text-white text-3xl">mail</span>
            </div>
            <h2 class="text-3xl md:text-4xl font-bold text-white mb-4 font-display">Stay in the Loop</h2>
            <p class="text-white/60 mb-8 max-w-lg mx-auto">Get the latest stories, exclusive interviews, and critical analysis delivered straight to your inbox.</p>
            <form class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
                <input class="flex-1 bg-white/5 border border-white/10 rounded-xl px-5 py-3 text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all" placeholder="Enter your email" type="email"/>
                <button class="bg-primary hover:bg-primary/90 text-white font-bold py-3 px-6 rounded-xl transition-colors shadow-lg shadow-primary/25" type="button">
                    Subscribe
                </button>
            </form>
        </div>
    </section>

    <footer class="border-t border-white/5 bg-background-dark/80 backdrop-blur-xl">
        <div class="max-w-[1280px] mx-auto px-6 py-12">
            <div class="flex flex-col md:flex-row justify-between items-center gap-6">
                <div class="flex items-center gap-2">
                    <div class="bg-primary/20 p-1.5 rounded-full">
                        <span class="material-symbols-outlined text-primary text-sm">diamond</span>
                    </div>
                    <span class="font-display font-bold text-lg text-white">CREATIVE.</span>
                </div>
                <div class="flex gap-6">
                    <a class="text-white/40 hover:text-white transition-colors" href="#">Twitter</a>
                    <a class="text-white/40 hover:text-white transition-colors" href="#">LinkedIn</a>
                    <a class="text-white/40 hover:text-white transition-colors" href="#">Instagram</a>
                </div>
            </div>
            <p class="text-center text-white/40 text-sm mt-8">© 2025 Phan Huu Hieu. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>